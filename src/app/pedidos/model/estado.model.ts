export interface Estado {
    id: number;       // Identificador único del estado
    nombre: string;   // Nombre descriptivo del estado (ejemplo: "Pendiente", "En camino", "Entregado")
    color: string;    // Color asociado al estado (ejemplo: "danger", "warning", "success")
  }
  